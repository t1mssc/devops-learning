{{- /*
NOTES.txt for devops-pipeline-time-printer
This template is used to print helpful info after Helm install.
*/ -}}

{{- if .Values.httpRoute }}
HTTP Route Info:
  Enabled: {{ .Values.httpRoute.enabled | default false }}

  {{- $parents := default list .Values.httpRoute.parentRefs }}
  {{- if gt (len $parents) 0 }}
  Parent References:
    {{- range $i, $ref := $parents }}
    - {{ $ref }}
    {{- end }}
  {{- else }}
  No parentRefs defined.
  {{- end }}

{{- else }}
HTTP Route not configured in values.yaml
{{- end }}

Application deployed successfully.